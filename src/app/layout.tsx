import type { Metadata } from "next";
import { Inter, JetBrains_Mono } from "next/font/google";
import { Providers } from "@/components/providers";
import "./globals.css";

const sans = Inter({
  variable: "--font-sans",
  subsets: ["latin"],
  display: "swap",
});

const mono = JetBrains_Mono({
  variable: "--font-mono",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "ResumeChecker — AI-Powered Resume Analysis",
  description:
    "Check your resume like a recruiter. AI-powered ATS & recruiter analysis with scores, feedback, and a clear verdict. 100% private — your data never leaves your browser.",
  openGraph: {
    title: "ResumeChecker — AI-Powered Resume Analysis",
    description:
      "AI-powered ATS & recruiter analysis. Free, private, open source.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${sans.variable} ${mono.variable} h-full antialiased`}
      suppressHydrationWarning
    >
      <body className="min-h-full flex flex-col">
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
